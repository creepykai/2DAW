<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<body>
    <!---->
    <script>
        // function saludar(nombre, callback){
        //     console.log("Hola " + nombre);
        //     callback(nombre);
        // }

        // function despedir(nombre){
        //     console.log("Adios " + nombre);
        // }

        // saludar("Irene", despedir);

        // function operacionAsincrona(callback){
        //     setTimeout(function(){
        //         console.log("Operacion asincrona completada");
        //         callback();
        //     }, 2000);
        // }

        // operacionAsincrona(() => {
        //     console.log("Operacion asincrona completada");
        // });

        // operacionAsincrona(despedir);

        //Promesas
        const doTask = () => {
            return new Promise((resolve, reject) => {
                const number = 1 + Math.floor(Math.random() * 6);
                if(number == 6){
                    resolve(number);
                }

                reject(number);
            })
        }

        doTask()
            .then(number => console.log("Success: " + number))
            .catch(number => console.log("Error: " + number));

        
    </script>
</body>
</html>
